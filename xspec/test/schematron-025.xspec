<?xml version="1.0" encoding="UTF-8"?>
<x:description schematron="schematron/schematron-025.sch" xmlns:x="http://www.jenitennison.com/xslt/xspec">

	<!-- Based on https://github.com/xspec/xspec-maven-plugin-1/issues/22#issuecomment-616780084 -->

	<x:param name="allow-foreign">true</x:param>

	<x:scenario
		label="Test sch:report which depends on a function included by a relative URI in a foreign (non Schematron) element">
		<x:context>
			<foo>bad</foo>
		</x:context>
		<x:expect-report id="report-bad-text" />
	</x:scenario>

</x:description>
